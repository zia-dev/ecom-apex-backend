export interface CreateDto {
  // service_id: string;
  document_name: string;
  document_path: string;
}
export interface UpdateDto {
  // service_id: string;
  document_name?: string;
  document_path?: string;
}